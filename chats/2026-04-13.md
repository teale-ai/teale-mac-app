# 2026-04-13

## Session: Commit Persistence Changes + Cofounder WAN Setup

### What was done

**Part 1: Commit & Merge**
- Committed settings persistence changes (LAN/WAN toggles, auto-manage, maxStorage fix, last-loaded model auto-reload) that were left uncommitted from the prior session.
- Rebased onto `main` (resolved one merge conflict in AppState.swift — kept both the WAN log line and the UserDefaults persistence line).
- Verified `swift build` passes.
- Created and merged PR #29 to `main`.
- Branch `taylorhou/usdc-more-decimals` deleted on remote.

**Part 2: Mac Studio Redeployment**
- Built Teale.app from latest `main` via `./bundle.sh`.
- Deployed to Mac Studio (`tailor64@tailor64s-mac-studio`) at `/Applications/Teale.app`.
- Confirmed auto-reload of Qwen3-32B-4bit on restart (persistence feature working).
- Enabled WAN via `defaults write` since it wasn't persisted before the new code.
- Restarted and verified: API healthy, model loaded, WAN enabled, cluster enabled.

**Part 3: Cofounder Setup (Prepared)**
- The same `.build/Teale.app` works on any Apple Silicon Mac (MacBook Air included).
- Cofounder needs: install Teale.app → enable WAN → discovers Mac Studio via relay → chat routes through WAN → USDC billing happens automatically.
- No code changes needed — the WAN relay infrastructure, USDC billing, and P2P connections all exist.

### Key Decisions
- WAN was already functional but the toggle wasn't persisted on the Mac Studio because the old binary didn't have the persistence code. Fixed by setting defaults directly.
- Confirmed `hasAvailableInferenceTarget` already handles WAN peers — the macOS Chat view can route inference to discovered WAN peers seamlessly.

### Issues
- The Mac Studio's `teale.wanEnabled` wasn't set because the prior session enabled WAN before the persistence code existed. Fixed by writing the default manually.
